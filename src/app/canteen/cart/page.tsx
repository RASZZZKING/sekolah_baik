import CartShopPage from "@/components/cart";
import { FunctionComponent } from "react";

interface pageProps {}

const page: FunctionComponent<pageProps> = () => {
  return <CartShopPage />;
};

export default page;
